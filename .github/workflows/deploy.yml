name: Deploy to cPanel (DISABLED - now using Coolify)

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deployment now handled by Coolify via Dockerfile. This workflow is disabled."
